apiVersion: v1
kind: Service
metadata:
  name: {{ include "nextjs-charts.fullname" . }}
  labels:
    {{- include "nextjs-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nextjs-charts.selectorLabels" . | nindent 4 }}
